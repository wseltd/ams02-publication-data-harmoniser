# API Reference

Auto-generated by `scripts/gen_api_ref.py` — do not edit manually.

## ams02wb.crawler

### ams02wb.crawler.attachment_resolver

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `Attachment` (class) | `(url: 'str', filename: 'str', content_type: 'str') -> None` | A downloadable file linked from a publication page. | none |
| `resolve_attachments` | `(html: 'str', base_url: 'str') -> 'list[Attachment]'` | Extract CSV and PDF attachment links from *html*. | none |

### ams02wb.crawler.index_loader

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `PublicationEntry` (class) | `(paper_id: 'str', title: 'str', url: 'str') -> None` | A single publication from the AMS-02 index page. | none |
| `load_publication_index` | `(session: 'object', index_url: 'str') -> 'List[PublicationEntry]'` | Fetch the publication index page and return a list of PublicationEntry. | none |

### ams02wb.crawler.page_fetcher

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `FetchedPage` (class) | `(html: 'str', base_url: 'str') -> None` | Raw result of fetching a publication page. | none |
| `fetch_publication_page` | `(session: 'object', page_url: 'str') -> 'FetchedPage'` | Fetch a publication page and return its HTML with the final resolved URL. | none |

## ams02wb.parsers

### ams02wb.parsers.context

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `ParseContext` (class) | `(*, stat_err_from_table: bool = False, sys_err_from_table: bool = False, sys_err_symmetrised: bool = False, err_split_heuristic: bool = False) -> None` | Flags indicating how each uncertainty was obtained during parsing. | none |

### ams02wb.parsers.csv_parser

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `Measurement` (class) | `(species: 'str', rigidity_gv_low: 'float', rigidity_gv_high: 'float', value: 'float', stat_err_plus: 'float', stat_err_minus: 'float', sys_err_plus: 'float', sys_err_minus: 'float') -> None` | A single row of harmonised AMS-02 measurement data. | none |
| `ParsedTable` (class) | `(measurements: 'List[Measurement]', source_columns: 'List[str]', header_row_index: 'int', provenance: 'Provenance') -> None` | Result of parsing a single CSV table. | passes_through |
| `Provenance` (class) | `(paper_id: 'str', file_url: 'str') -> None` | Tracks where a parsed table came from. | none |
| `parse_csv` | `(source: 'IO[str]') -> 'list[dict]'` | Parse a CSV from a file-like object into a list of dicts. | none |
| `parse_csv_table` | `(file_path: 'Path', paper_id: 'str', file_url: 'str') -> 'ParsedTable'` | Parse an AMS-02 CSV file into a ParsedTable. | writes_provenance |

### ams02wb.parsers.pdf_parser

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `extract_first_numeric_table` | `(tables: 'list[list[list[str]]]') -> 'list[list[str]] \| None'` | Return the first table that contains at least one numeric row. | none |
| `extract_tables` | `(pdf_path: 'Path \| str') -> 'list[list[list[str]]]'` | Extract all tables from a PDF file using pdfplumber. | none |
| `map_columns` | `(headers: 'list[str]') -> 'dict[str, str]'` | Map PDF table headers to canonical schema field names. | none |
| `map_pdf_rows_to_measurements` | `(rows: 'list[list[str]]') -> 'list[dict[str, Any]]'` | Convert pre-extracted PDF table rows to canonical Measurement dicts. | none |

## ams02wb.schema

### ams02wb.schema.axes

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `AxisName` (class) | `(*values)` | Canonical axis identifiers used throughout the harmoniser. | none |
| `list_known_labels` | `() -> 'list[str]'` | Return a sorted list of all recognised axis label aliases. | none |
| `normalise_axis_label` | `(raw: 'str') -> 'AxisName'` | Map a raw axis label string to the canonical AxisName. | none |

### ams02wb.schema.dataset_validator

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `FileResult` (class) | `(…)` | Validation result for a single dataset file. | none |
| `validate_dataset_dir` | `(input_dir: 'Path') -> 'List[FileResult]'` | Validate all JSON files in a directory. | none |
| `validate_dataset_file` | `(path: 'Path') -> 'FileResult'` | Validate a single JSON dataset file. | none |

### ams02wb.schema.models

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `Measurement` (class) | `(*, energy_low: float = 0.0, energy_high: float = 0.0, energy_mid: float = 0.0, value: float = 0.0, unit: str = 'GeV', axis_type: str = 'kinetic_energy_per_nucleon', species: str = 'PROTON', stat_error_low: float \| None = None, stat_error_high: float \| None = None, sys_error_low: float \| None = None, sys_error_high: float \| None = None, stat_err_pos: float \| None = None, stat_err_neg: float \| None = None, sys_err_pos: float \| None = None, sys_err_neg: float \| None = None, stat_err_label: ams02wb.schema.models.UncertaintyLabel \| None = None, sys_err_label: ams02wb.schema.models.UncertaintyLabel \| None = None, time_start: str \| int \| float \| None = None, time_end: str \| int \| float \| None = None, time_start_utc: datetime.datetime \| None = None, time_end_utc: datetime.datetime \| None = None) -> None` | A single data point from an AMS-02 publication table. | none |
| `ProvenanceRecord` (class) | `(*, paper_doi: str, table_id: str, file_url: str, ingested_at: str, source_type: str) -> None` | Tracks the origin of an ingested dataset. | none |
| `Species` (class) | `(*, name: str, charge_z: int, mass_gev: float, mass_number_a: int) -> None` | Physical species properties needed for axis conversions. | none |
| `UncertaintyLabel` (class) | `(*values)` | How an uncertainty value was obtained. | none |

### ams02wb.schema.provenance

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `ProvenanceRegistry` (class) | `() -> 'None'` | Collects ProvenanceRecord instances keyed by dataset_id. | none |
| `ProvenanceRegistry.add` | `(self, dataset_id: 'str', record: 'ProvenanceRecord') -> 'None'` | Register *record* under *dataset_id*. | none |
| `ProvenanceRegistry.get` | `(self, dataset_id: 'str') -> 'ProvenanceRecord \| None'` | Return the record for *dataset_id*, or ``None`` if absent. | none |
| `ProvenanceRegistry.list_ids` | `(self) -> 'list[str]'` | Return all registered dataset_ids. | none |
| `ProvenanceRegistry.to_dict` | `(self) -> 'dict[str, dict]'` | Serialise all entries to ``{dataset_id: dict}``. | none |

### ams02wb.schema.units

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `convert_energy` | `(value: 'float', from_unit: 'str', to_unit: 'str') -> 'float'` | Convert an energy value between MeV, GeV, and TeV. | none |
| `convert_flux` | `(value: 'float', from_unit: 'str', to_unit: 'str') -> 'float'` | Convert a flux value between per-m² and per-cm² area normalisation. | none |
| `kinetic_energy_to_rigidity` | `(ek_per_nucleon_gev: 'float', charge_z: 'int', mass_gev: 'float', mass_number_a: 'int' = 1) -> 'float'` | Convert kinetic energy per nucleon to magnetic rigidity. | none |
| `rigidity_to_kinetic_energy` | `(rigidity_gv: 'float', charge_z: 'int', mass_gev: 'float', mass_number_a: 'int' = 1) -> 'float'` | Convert magnetic rigidity to kinetic energy per nucleon. | none |

### ams02wb.schema.validators

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `ValidationFinding` (class) | `(field: str, value: Any, reason: str)` | A single validation problem found in a record. | none |
| `validate_energy_fields` | `(record: Dict[str, Any]) -> List[ams02wb.schema.validators.ValidationFinding]` | Validate energy fields for presence, type, bounds, and ordering. | none |
| `validate_flux_fields` | `(record: Dict[str, Any]) -> List[ams02wb.schema.validators.ValidationFinding]` | Validate flux field for presence, type, and physical bounds. | none |
| `validate_record` | `(record: Dict[str, Any]) -> List[ams02wb.schema.validators.ValidationFinding]` | Run all validators on a single record, collecting all findings. | none |
| `validate_uncertainty_fields` | `(record: Dict[str, Any]) -> List[ams02wb.schema.validators.ValidationFinding]` | Validate statistical uncertainty fields for presence, type, and sign. | none |

## ams02wb.harmoniser

### ams02wb.harmoniser.axis

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `harmonise_axes` | `(measurements: 'list[Measurement]', target_energy_unit: 'str' = 'GeV', target_axis: 'str' = 'kinetic_energy_per_nucleon') -> 'list[Measurement]'` | Convert measurements to a common energy axis and unit. | none |

### ams02wb.harmoniser.pipeline

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `run_harmonisation_pipeline` | `(measurements: 'list[Measurement]', parse_context: 'ParseContext', provenance_json: 'dict') -> 'list[dict]'` | Apply all four harmonisation stages and return canonical-schema dicts. | passes_through |

### ams02wb.harmoniser.species

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `get_species` | `(name: 'str') -> 'Species'` | Look up species properties by name or alias. | none |
| `normalise_species` | `(name: 'str') -> 'str'` | Map a species name or alias to its canonical uppercase form. | none |

### ams02wb.harmoniser.timewindow

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `GapWarning` (class) | `(index_a: 'int', index_b: 'int', gap_days: 'float') -> None` | Warning for gaps between consecutive measurement time windows. | none |
| `OverlapWarning` (class) | `(index_a: 'int', index_b: 'int', overlap_days: 'float') -> None` | Warning for overlapping measurement time windows. | none |
| `bartels_to_utc` | `(rotation: 'int') -> 'datetime'` | Convert a Bartels rotation number to a UTC datetime. | none |
| `detect_gaps` | `(measurements: 'list[Measurement]', threshold_days: 'float' = 1.0) -> 'list[GapWarning]'` | Detect gaps between consecutive time windows that exceed a threshold. | none |
| `detect_overlaps` | `(measurements: 'list[Measurement]') -> 'list[OverlapWarning]'` | Detect overlapping time windows in a list of measurements. | none |
| `normalise_time_window` | `(measurement: 'Measurement') -> 'Measurement'` | Return a new Measurement with time_start_utc and time_end_utc set. | none |
| `parse_time_window` | `(start: 'str \| int \| float \| None', stop: 'str \| int \| float \| None') -> 'tuple[datetime \| None, datetime \| None]'` | Parse a start/stop pair into timezone-aware UTC datetimes. | none |
| `to_bartels_rotation` | `(dt: 'datetime') -> 'int'` | Convert a UTC datetime to a Bartels rotation number. | none |

### ams02wb.harmoniser.uncertainty

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `label_uncertainties` | `(measurement: 'Measurement', parse_context: 'ParseContext') -> 'Measurement'` | Return a new Measurement with stat_err_label and sys_err_label set. | none |

## ams02wb.likelihood

### ams02wb.likelihood.diagonal

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `build_diagonal_covariance` | `(stat_err: numpy.ndarray, sys_err_total: numpy.ndarray \| None = None) -> numpy.ndarray` | Build a diagonal covariance matrix from statistical and systematic errors. | none |
| `diagonal_chi2` | `(residuals: numpy.ndarray, stat_err: numpy.ndarray, sys_err_total: numpy.ndarray \| None = None) -> float` | Compute chi-squared using diagonal covariance. | none |

### ams02wb.likelihood.fitready

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `build_fit_dataset` | `(y: 'np.ndarray', x: 'np.ndarray', covariance: 'np.ndarray', uncertainty_label: 'str', mode: 'str', provenance: 'dict', species: 'str' = '', x_axis_type: 'str' = '', y_unit: 'str' = '') -> 'dict'` | Bundle validated arrays and metadata into a fit-ready dataset dict. | passes_through |
| `build_fit_dataset_from_measurement` | `(measurements: 'list[Measurement]', covariance: 'np.ndarray', uncertainty_label: 'str', mode: 'str', provenance: 'dict', x_axis_type: 'str' = '', y_unit: 'str' = '') -> 'dict'` | Build a fit-ready dataset directly from Measurement objects. | writes_provenance |

### ams02wb.likelihood.grouped_systematic

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `build_grouped_systematic_covariance` | `(stat_err: numpy.ndarray, sys_err: numpy.ndarray) -> numpy.ndarray` | Build a covariance matrix with uncorrelated stat and correlated sys. | none |

### ams02wb.likelihood.kernel_covariance

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `build_kernel_covariance` | `(stat_err: numpy.ndarray, x: numpy.ndarray, corr_length: float) -> numpy.ndarray` | Build a covariance matrix with squared-exponential kernel correlations. | none |

## ams02wb.alignment

### ams02wb.alignment.bartels

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `align_bartels_rotation` | `(df: 'pd.DataFrame') -> 'dict[int, pd.DataFrame]'` | Group measurements by Bartels rotation number. | none |
| `bartels_rotation_number` | `(date: 'datetime.date') -> 'int'` | Compute the Bartels rotation number for a calendar date. | none |
| `bartels_rotation_start` | `(rotation: 'int') -> 'datetime.date'` | Return the start date (first day) of the given Bartels rotation. | none |

### ams02wb.alignment.daily

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `DailyAlignedResult` (class) | `(aligned_df: 'pd.DataFrame', missing_counts: 'dict[str, int]', join_type: 'str', date_range: 'tuple[datetime.date, datetime.date]') -> None` | Result of aligning daily species time-series. | none |
| `align_daily_series` | `(species_frames: 'dict[str, pd.DataFrame]', *, join: "Literal['inner', 'outer']" = 'inner') -> 'DailyAlignedResult'` | Align multiple species DataFrames on their ``time_start`` date column. | none |

### ams02wb.alignment.diagnostics

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `JoinDiagnostics` (class) | `(coverage_fraction: 'dict[str, float]', gap_histogram: 'dict[str, list[int]]', overlap_count: 'int') -> None` | Diagnostics for an aligned multi-species DataFrame. | none |
| `compute_join_diagnostics` | `(aligned_df: 'pd.DataFrame', species_list: 'list[str]') -> 'JoinDiagnostics'` | Compute join diagnostics for an aligned species DataFrame. | none |

## ams02wb.exports

### ams02wb.exports.csv_export

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `export_csv` | `(measurements: 'list[Measurement]', path: 'Path') -> 'Path'` | Write measurements to a CSV file in canonical schema order. | none |

### ams02wb.exports.json_export

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `AMS02Encoder` (class) | `(*, skipkeys=False, ensure_ascii=True, check_circular=True, allow_nan=True, sort_keys=False, indent=None, separators=None, default=None)` | JSON encoder that handles numpy arrays, numpy scalars, and datetimes. | none |
| `AMS02Encoder.default` | `(self, obj: 'Any') -> 'Any'` | Serialise numpy and datetime objects to JSON-compatible types. | none |
| `export_json` | `(data: 'dict[str, Any]', path: 'Path', *, indent: 'int \| None' = 2) -> 'Path'` | Write a data dictionary to a JSON file using AMS02Encoder. | none |

### ams02wb.exports.parquet

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `export_parquet` | `(dataset: 'dict[str, Any]', path: 'str \| Path') -> 'Path'` | Write a fit-ready dataset dict to a parquet file. | reads_provenance |

### ams02wb.exports.usine_export

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `build_usine_quantity_string` | `(species_num: 'str', species_den: 'str \| None') -> 'str'` | Build a USINE quantity string from numerator and optional denominator. | none |
| `export_usine` | `(measurements: 'list[Measurement]', *, species_num: 'str', species_den: 'str \| None' = None, x_axis_type: 'str' = 'kinetic_energy_per_nucleon', experiment: 'str' = 'AMS-02', bibcode: 'str \| None' = None) -> 'str'` | Export measurements to a USINE-format string. | none |

## ams02wb.cli

### ams02wb.cli.ingest_all_runner

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `IngestSummary` (class) | `(succeeded: 'list[str]' = <factory>, failed: 'list[dict]' = <factory>, total: 'int' = 0) -> None` | Result of a batch ingestion run. | none |
| `run_ingest_all` | `(index_path: 'Path', output_dir: 'Path') -> 'IngestSummary'` | Ingest every publication listed in a local publication_index.json. | none |

### ams02wb.cli.ingest_publication

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `run_single_ingest` | `(session: 'requests.Session', entry: 'PublicationEntry', output_dir: 'Path') -> 'dict'` | Ingest one publication: fetch page, resolve attachments, download, write JSON. | none |

### ams02wb.cli.provenance

| Function/Class | Signature | Description | Provenance Note |
|---|---|---|---|
| `attach_provenance` | `(records: 'list[dict]', provenance: 'dict') -> 'list[dict]'` | Inject a JSON-serialised provenance field into every record. | reads_provenance |
| `build_provenance_json` | `(publication_id: 'str', publication_url: 'str', table_id: 'str', source_file_url: 'str', source_file_format: 'str', parse_method: 'str', parse_version: 'str', retrieval_timestamp: 'str', content_hash: 'str') -> 'dict[str, str]'` | Return a dict containing all nine provenance fields. | passes_through |
| `compute_content_hash` | `(data: 'bytes') -> 'str'` | Return the SHA-256 hex digest of *data*. | none |

## Missing Docstrings

All public functions and classes have docstrings.
